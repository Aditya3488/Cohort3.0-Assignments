// # 16. join()

// ### Intermediate

// Convert array into comma separated string.

// **Hint:** Use `join(",")`

// ### Hard

// Convert array into sentence format.

// **Hint:** Join with spaces

var arr1 = ["Hello", "World", "from", "JavaScript"];
var commaSeparated = arr1.join(",");
console.log(commaSeparated);

var sentenceFormat = arr1.join(" ");
console.log(sentenceFormat);
